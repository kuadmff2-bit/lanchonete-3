const json=(data,status=200)=>new Response(JSON.stringify(data),{status,headers:{"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
export async function onRequestGet({env}){if(!env.PROMOTIONS)return json({active:false});const raw=await env.PROMOTIONS.get("current-promotion");if(!raw)return json({active:false});try{return json(JSON.parse(raw))}catch{return json({active:false})}}
export async function onRequestPost({request,env}){if(!env.PROMOTIONS)return json({error:"KV PROMOTIONS não configurado."},500);if(!env.ADMIN_PASSWORD)return json({error:"Senha de administrador não configurada."},500);const password=request.headers.get("x-admin-password")||"";if(password!==env.ADMIN_PASSWORD)return json({error:"Senha incorreta."},401);let data;try{data=await request.json()}catch{return json({error:"Dados inválidos."},400)}const promo={active:Boolean(data.active),title:String(data.title||"").trim().slice(0,80),description:String(data.description||"").trim().slice(0,240),image:String(data.image||""),updatedAt:new Date().toISOString()};if(promo.active&&!promo.title)return json({error:"Informe o título da promoção."},400);if(promo.image&&!promo.image.startsWith("data:image/"))return json({error:"Formato de imagem inválido."},400);if(promo.image.length>1500000)return json({error:"A imagem ficou muito grande. Escolha outra foto."},413);await env.PROMOTIONS.put("current-promotion",JSON.stringify(promo));return json({ok:true,promotion:promo})}
